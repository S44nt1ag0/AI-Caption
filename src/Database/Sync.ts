import User from "./Models/UserModel";
import Caption from "./Models/Captions";
import createRelation from "./Relation";

const databaseSync = () => {
//   User.sync({ alter: true });
//   Caption.sync({ alter: true });
//   createRelation();
};

export default databaseSync;
